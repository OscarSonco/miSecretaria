# Plantilla — copiar a secrets.properties (NO se commitea, está en .gitignore) y rellenar
# con los datos reales. Solo se usan si se compila con -PincludeSecrets=true
# (ver build_interna.sh); el build público (release.sh) nunca los lee.
botToken=
chatId=
